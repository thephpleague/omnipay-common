parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpunit:
            config_file: ~
            testsuite: ~
            group: []
            always_execute: false
        phpcs:
            standard: PSR2
            show_warnings: false
            ignore_patterns:
              - tests/
            triggered_by: [php]